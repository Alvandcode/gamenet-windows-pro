# آموزش خیلی ساده - نسخه ویندوز

### این چیه؟
همون برنامه گیم‌نت ولی برای کامپیوتر ویندوز. همه چیز مثل اندروید شده + 4 تا چیز جدید.

---

## قسمت 1: فایل جدید کجاست؟

توی کامپیوترت برو:
`Documents` -> `Default Project` -> `gamenet-windows-enhanced`

داخلش اینا رو میبینی:
- `index.html` (برنامه اصلی جدید)
- `main.js` (برای ویندوز)
- `package.json` (تنظیمات)
- `build-windows.bat` (دکمه ساخت)

---

## قسمت 2: چطوری جایگزین کنی؟ (مثل اندروید)

1. پوشه ویندوز قدیمی که از گوگل درایو دانلود کردی باز کن (همون `gamenet-electron-app`)
2. فایل `index.html` قدیمی رو یه کپی بگیر بذار دسکتاپ (بکاپ)
3. برو `gamenet-windows-enhanced` -> فایل `index.html` جدید رو کپی (Ctrl+C)
4. برو تو پوشه قدیمی و پیست (Ctrl+V) -> بگو جایگزین بشه
5. فایل `package.json` جدید رو هم همینطور کپی کن جای قدیمی

تمام! حالا ویندوزت هم جدید شد.

---

## قسمت 3: چطوری به برنامه قابل نصب (EXE) تبدیل کنی؟

### راه ساده روی خود کامپیوتر (بدون گیت‌هاب)

1. تو پوشه `gamenet-electron-app` (همون قدیمی که الان جدید شده) روی `build-windows.bat` **دوبار کلیک** کن
2. یه صفحه سیاه باز میشه صبر کن 5 تا 10 دقیقه (اینترنت میخواد)
3. آخرش مینویسه `Done! Check the dist folder`
4. برو داخل پوشه `dist` -> 2 تا فایل ساخته شده:
   - `Gamenet Manager Alvand Setup.exe` = اینو به مشتری بده نصب کنه (مثل بقیه برنامه‌ها Next -> Install)
   - `GamenetManager-Portable-1.1.0.exe` = این بدون نصب اجرا میشه (بذار رو دسکتاپ دوبار کلیک)

اگر ارور `npm not found` داد:
- برو `nodejs.org` -> دکمه سبز LTS رو دانلود و نصب کن -> دوباره `build-windows.bat` رو بزن

---

### راه با گیت‌هاب (که خودش EXE بسازه)

اگه میخوای مثل اندروید گیت‌هاب خودش برات بسازه:

1. برو `github.com` -> دکمه `New repository` -> اسم بذار `gamenet-windows` -> `Create`
2. تو صفحه جدید دکمه `uploading an existing file` رو بزن
3. همه فایل‌های داخل `gamenet-windows-enhanced` رو با ماوس بکش بنداز تو سایت (یا `choose your files`)
   - `index.html`, `main.js`, `package.json`, `build-windows.bat`, پوشه `.github`
4. پایین `Commit changes` رو بزن
5. بالا تب `Actions` رو بزن -> یه خط سبز میبینی داره میسازه (10 دقیقه طول میکشه چون ویندوزه)
6. بعدش تب `Releases` رو بزن -> آخرین نسخه -> فایل `Setup.exe` رو دانلود کن

**نکته:** برای ویندوز حتما باید `Actions` روی `windows-latest` باشه (ما داخل فایل `.github/workflows/build-windows.yml` درستش کردیم).

---

## قسمت 4: چطوری از قابلیت جدید استفاده کنی؟ (دقیقا مثل اندروید)

**رزرو:**
- منو `رزروها` -> `رزرو جدید` -> دستگاه + اسم مشتری + تاریخ + ساعت

**تایمر:**
- `مدیریت کلاینت‌ها` -> `مدیریت زمان` -> پایین `30د / 1س` رو بزن -> `شروع`

**PDF و ارسال:**
- روی هر دستگاه دکمه `PDF` یا `ارسال` -> انتخاب تلگرام/واتساپ/روبیکا/ایمیل

همه چیز عین اندروید کار میکنه.

---

## سوالی داری؟
فقط بگو کدوم قدم گیر کردی، همونجا رو ساده‌تر توضیح میدم.
